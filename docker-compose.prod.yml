version: "3.8"
services:
  front:
    build: 
      dockerfile: Dockerfile.prod
      context: ./front
    ports:
      - 80:80
  api:
    build:
      context: ./platform
    ports:
      - 8084:8084
